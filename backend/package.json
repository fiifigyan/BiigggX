{
  "name": "biigggx-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx convex dev",
    "deploy": "npx convex deploy"
  },
  "dependencies": {
    "@convex-dev/stripe": "^0.1.3",
    "convex": "^1.9.0",
    "stripe": "^14.17.0"
  }
}
